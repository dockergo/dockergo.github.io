# 全站内容优化诊断报告 + 逐一升级计划

> 交付物性质:**规划文档,不动任何生产代码**。经你审阅确认后再分批改造。
> 评估三维度:①技术广度/深度增强(**必须基于源码**);②图表达优于文本;③所有图符合 Apple 工业风。
> 生成日期:2026-08-14。范围:7 大内容域,约 1670 个 SVG + 470 个 MD。

---

## 一、全站规模与成熟度全景

| 内容域 | 单元数 | SVG | MD | 内容性质 | 是否可源码化 |
|---|---|---|---|---|---|
| projects/ | 38 真实项目 | 1428 | 429 | 真实开源项目源码剖面 | ✅ 强(有真实仓库) |
| topics/ | 6 主题 | 50 | 6 | 跨项目算法主题(概念层,点名项目) | ◐ 中(落点项目已核实) |
| principles/ | 8 slug | 40 | 10 | 系统设计原理(点名项目) | ◐ 中(_VERIFIED_FACTS 已约束) |
| llm-agent/ | 若干模块 | 76 | 12 | AI Agent 生态(概念层,不点名框架) | ◑ 弱(概念为主) |
| basic/ | 12 条目 | 73 | 13 | 数据结构算法(配 Go 代码) | ✅ 强(hello-algo 代码已校验) |
| llm/ | design 分散 | 0(散在子目录) | 0 | LLM 子专题 | ◑ 弱 |
| scenarios/ | 11 场景 | 1 | 0 | 业务场景架构 | ◑ 弱(业务抽象) |

**核心洞察**:这是一个高度成熟的仓库。**定位是「架构原理」而非「源码注释」**——深广度增强指深化架构机制、数据流、组件协作、设计权衡的表达,**不是往图里堆类名/行号**。源码只用于核实原理正确性。

---

## 二、维度一:技术深广度(基于源码核实,呈现为架构)—— 评估口径

> **重要更正**:此前用「SVG 源码行号锚点密度」作深度代理指标的分档方法**已作废**。当前所有项目是**架构原理**,不是源码注释,深度看的是**架构原理的完整度与洞察力**,不是行号数量。

**「技术广度和深度增强(必须基于源码)」的正确含义**:
- **深化对象**:机制、数据流、组件协作、状态流转、关键路径、设计权衡(为何这样设计、代价是什么)等**架构认知层**。
- **「基于源码」**:架构原理必须**正确、经得起源码验证**(禁编造机制),但**呈现形态是架构图,不是代码位置索引**。补的是理解深度,不是行号。
- **广度**:是否覆盖了该系统的关键子系统/主流程/边界情况;窄则补维度。

**如何判断某项目深广度是否不足**(需实际看内容,非机械指标):
1. 机制图是否只画了"有哪些组件",还缺"数据如何流动、状态如何转换"?
2. 是否缺**设计权衡**表达(如 CMP 对比图:为何选 A 不选 B、代价矩阵)?
3. 是否缺关键路径的**分步骤时序/流程**?
4. 广度上是否漏掉该系统的重要子系统(如只讲存储没讲事务/恢复)?

→ **动作**:逐项目走查 design/ 下 SVG+MD,识别"停留在组件罗列、缺机制深化/权衡/数据流"的图,用**架构图**补深度与广度。principles/topics 严格遵循 `_VERIFIED_FACTS.md`,概念参考项保留诚实标注。

---

## 三、维度二:图优于文本 —— 全站已达标(2026-08-14)

**结论:全站图文比已达标,无真实缺图段落。**

诊断经两轮口径纠错后定案:
- **假信号1**:「MD 内嵌 img 数」统计出大量 img=0,实为口径错误——非项目域(basic/topics/principles/llm-agent)的图全是独立 SVG,由 gen.py 按 @marker 锚点渲染进 HTML,从不内嵌 MD。
- **假信号2**:「锚点数 vs 同级 SVG 数」误报 foundations 6 模块 gap=+6,实为 SVG 在 <slug>/design/ 子目录未被统计;修正递归查找后缺口清零。
- **定案口径**:「内容锚点分节数 vs (目录+design/子目录) SVG 数」。全站 gap 全部 ≤0,每个内容分节都有配图,部分域 SVG 还多于锚点(consensus 17图/11锚点、动态规划 7图/6锚点)。
- **项目域**:doris 执行引擎 MD 等抽查均为图文比标杆。

→ 无需补图动作。表格作为图的合理补充予以保留。

---

## 四、维度三:Apple 工业风一致性

`_STYLE_GUIDE.md` 已定义严格规范(配色八家族、判型标题带、圆角阶梯、字号阶梯、CMP 版式)。标杆 SVG(kafka KRaft)完全合规。

**需巡检项**:
1. **背景色**:必须 `#fbfbfd` 纯白(深色由外层 CSS 反色),禁 SVG 内自造深色版。
2. **配色越界**:禁用八家族外的自造色值(暗色反色会露白 —— 见 memory 多条教训)。
3. **判型标题带**:每图必须有"结论先行"主标题 + 机制副标题 + 图例。
4. **字号/圆角阶梯**:统一到规范档位。
5. **内联 SVG id 命名空间**:同页多图 defs id 须唯一前缀(memory 教训)。

→ **动作**:批量扫描 SVG 的 `fill=` 色值白名单校验 + 背景色校验 + 标题带存在性校验,输出越界清单再逐一订正。

---

## 五、分批改造计划(优先级排序)

### 维度三(Apple 工业风)· ✅ 已完成(2026-08-14)
全站 1590 张正文机制图校验:背景色异常=0、深色底真问题=0。特例已豁免(icon.svg/总架构品牌图/doris 紧凑流程带/CMP 深色表头,见 memory)。**判定达标**。

### 第 1 批 · 深广度架构增强(最高价值)· 进行中
走查各项目 design/,识别"停留在组件罗列、缺机制深化/数据流/设计权衡"的图,用**架构图**补深度与广度(**不补类名/行号**,源码仅用于核实原理正确)。
每项目:走查 SVG+MD → 定位深度缺口 → 用架构图补机制/权衡/数据流 → `python3 gen.py` 重生验证。
按内容基数与影响力排序,首个样板项目由用户指定。

### 第 2 批 · 图优于文本(全域 MD 巡检)
识别"≥3 项并列/有流程/有对比"的纯文本块 → 升级为 SVG(对比矩阵/流程/关系图);表格是图的合理补充,非一律替换。

### 第 3 批 · 非项目域深化
topics(6)/principles(8) 对照 `_VERIFIED_FACTS.md` 补强架构原理;llm-agent/llm/scenarios 概念层结构关系图精确化。

### 第 4 批 · 全站重生成回归
全站 `python3 gen.py` 回归 + 8 主题色验证 + 无露白检查。

---

## 六、执行纪律(每批通用)

1. **架构原理非源码注释**:深化机制/数据流/权衡的表达,**不往图里堆类名/行号**(memory 铁律)。
2. **源码用于核实**:任何机制断言前先 grep 真实源码核实正确性;拿不准标"概念参考"。
3. **SVG=唯一真源**:项目页热区从 SVG data-tid 派生,改机制先改 SVG。
4. **单项目闭环**:改 → `python3 gen.py` 重生 → 验证(8 主题/无露白/无泄漏)→ 再下一个。
5. **不破坏既有高水位**:标杆项目勿过度改。
6. **旧式项目导航底图保留**(memory 铁律)。
7. **中文字符串禁弯双引号**(用「」,memory 铁律)。

---

## 七、当前进度

- ✅ **维度三 Apple 工业风**:全站达标(2026-08-14)。
- 🔶 **维度一 深广度架构增强**:已暂停——走查 doris(全站深度标杆)证实其非组件罗列,含机制+数据流+设计权衡,用户裁决整体暂停。方向已纠正为「架构原理非源码注释」。
- ✅ **维度二 图优于文本**:全站达标(2026-08-14)——两轮口径纠错后确认无真实缺图段落。

---

## 八、性能优化(远程访问加载速度)· ✅ 全站已完成(2026-08-14)

> 独立于三维内容优化的**加载性能**改造。用户裁决:①服务器压缩做不了 → 选 ②base64 图外链化 + ③CSS 抽外部;JS 不抽。

### 方案与工具
- **产物后处理**:新增 [`optimize_assets.py`](optimize_assets.py) 纯二次加工 gen.py 产物,**零破坏生成逻辑、可回滚**(重跑 gen.py + 本脚本即恢复)。
- **② base64 图外链化**:内联 `data:image/svg+xml;base64,` 解码 → 按 sha1[:16] 命名落盘 `assets/img/<hash>.svg`,全站哈希去重共享;引用改外链,`<img>` 追加 `loading="lazy"`。
- **③ CSS 抽外部**:各页 `<style>` 按内容哈希分组抽 `assets/css/<hash>.css`,页内改单一 `<link>`;跨页共享缓存。
- **JS 不抽**:43 种签名高度页面特化(各项目页含独立热区坐标),抽出无跨页缓存收益,放弃。

### 成果(全站 87 页)
| 指标 | 优化前 | 优化后 | 降幅 |
|---|---|---|---|
| 全站 HTML 总体积 | 22.0 MB | 7.28 MB | **-67%** |
| 首页 index.html | 1.40 MB | 509 KB | -64% |
| doris/index.html | 1.25 MB | 489 KB | -61% |
| 外链去重 SVG | — | 1580 个(assets/img) | 跨页共享缓存 |
| 共享 CSS | 内联×85 | 12 个(assets/css) | 跨页共享缓存 |

- 63 页外链改写、1510 处引用;85 页 CSS 抽离。

### 安全边界(铁律)
1. **JS 动态拼接 base64 保持内联**:9 个项目页的 `QSTOUR_OVERVIEW_B64`(走查图 overview,`src="data:...base64,'+变量`)脚本正则安全跳过,**不可外链**,图仍正常显示。
2. **纯固定色图标才可外链**:首页 148 个 `<image>` 经验证全纯固定色(0 currentColor/var),可安全外链;**需主题变色的 hero 内联 svg 绝不能外链**。
3. **base64-img 暗色靠 CSS `filter:invert(...)`**,外链 `<img>` 仍生效(9 个 css 含 invert 规则)。
4. **重跑 gen.py 会覆盖优化版** → 任何项目改 design/SVG 重生成后,须再跑 `python3 optimize_assets.py` 恢复外链(固有依赖,gen.py 后多一步)。

### 验证(全站抽样全通过)
- 残留 base64=9 页各 1 处(全为 JS 动态图,正确跳过)。
- 大页(clickhouse/go/kubernetes)外链图路径 0 缺失;内联 `<style>` 残留 0 页。
- 本地 HTTP:页面/共享 CSS/外链 SVG 均 200。

### 回滚
- 全站备份:`.html_backup_before_optimize.tgz`(优化前 87 页快照)。
- 单页备份:`index.html.bak`、`projects/doris/index.html.bak`。
- [`optimize_assets.py`](optimize_assets.py) 长期保留,供后续 gen.py 重生成后复用。

---

## 九、doris 双维模型图文字重叠修复 · ✅ 已完成(2026-08-14)

`projects/doris/design/Doris原理_双维模型.svg` 右侧后台 carrier 两行竖排标题 x 坐标均=1060 重叠 → 拉开为主标题 x=1052 / 副标题 x=1074(差 22px),y 统一 374 居中。重跑 gen.py + optimize_assets.py 生效,用户确认通过。