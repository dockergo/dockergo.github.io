# Redis 源码核实台账（内部工作笔记，非 design 产物）

源码：`~/workdir/redis` unstable @9e5614d（2026-07）。此树比标准 Redis 新：
IO 线程用 per-thread event loop（iothread.c）、Multi-Part AOF、OBJ_ARRAY 类型、subkey 通知等 fork 增补 —— 文档以此树为准，非标准项标注或回避。

## 对象与编码（object.h/object.c/dict.c/listpack.c/t_zset.c/intset.c/sds.h）
- redisObject: object.h:100-112，type:4 encoding:4 refcount(23bit) lru:24。类型常量 server.h:862-891（OBJ_STRING0/LIST1/SET2/ZSET3/HASH4/MODULE5/STREAM6/ARRAY7）。编码常量 object.h:75-88。
- String: EMBSTR_SIZE_LIMIT=44 (object.c:337)；≤44 embstr 否则 raw；≤20字可解析→int；共享整数 0-9999 (server.h:126)。
- Hash: hash-max-listpack-entries=512, -value=64 (config.c:3371,3377)；转换 t_hash.c:620-631。
- List: list-max-listpack-size=-2 (config.c:3293)=8KB/节点；quicklist optimization_level {4K,8K,16K,32K,64K}(quicklist.c:49)。
- Set: intset-entries=512, listpack-entries=128, listpack-value=64 (config.c:3372-3374)；t_set.c:47-80。
- ZSet: listpack-entries=128, -value=64 (config.c:3375,3379)；skiplist 转换 t_zset.c:1655-1659。
- dict: dict.h:162-178，ht[2]、rehashidx；渐进 rehash dictRehash 每次 n 桶+最多 n*10 空桶 (dict.c:405-431)；force_resize_ratio=4 (dict.c:45)；dictScan 反向二进制 (dict.c:1579-1586)。初始 size=4。
- listpack: LP_HDR_SIZE=6（32bit总字节+16bit元素数），LP_EOF=0xFF；每 entry = encoding+data+backlen，backlen 存自身长度→可反向遍历，解决 ziplist 级联更新 (listpack.c:27-90)。
- skiplist: ZSKIPLIST_MAXLEVEL=32, P=0.25 (server.h:645-646)；node 有 score/backward/level[]{forward,span}，span 用于 rank；zslRandomLevel while(rand<P*MAX)level++ (t_zset.c:254-260)。zset={dict+skiplist}。
- quicklist: quicklist.h:47-117，node 是 listpack，LZF 压缩，compress 深度留头尾不压。
- intset: 有序 int 数组，INT16/32/64，升级单向 (intset.c)。
- sds: sdshdr5/8/16/32/64 (sds.h:57-62)，header 随长度选宽，len+alloc+flags+buf，packed。

## 网络与执行（ae.c/networking.c/server.c/iothread.c）
- ae: aeEventLoop (ae.h:79-93)；后端 evport>epoll>kqueue>select (ae.c:32-42)；aeMain (ae.c:497)；aeProcessEvents 顺序 beforesleep→poll→aftersleep→file events→timers (ae.c:365-473)；AE_BARRIER 反转读写序（beforeSleep fsync 先于 reply）。
- 单线程执行：call()→cmd->proc(c) (server.c:4024)，仅主线程。IO 线程不 call()。
- IO 线程：io-threads 默认=1 (config.c:3289)；每 IO 线程有自己的 event loop IOThreadMain (iothread.c:860)；只做 读+解析+写回，命令执行仍主线程串行；client 绑定线程 (client.tid)；io-threads-do-reads 已废弃 (config.c:452)。
- 客户端流：accept→createClient (networking.c:122，注册 readQueryFromClient)→readQueryFromClient (networking.c:3835，PROTO_IOBUF_LEN=16KB)→processInputBuffer (networking.c:3631)→processCommand→call。reply: 静态 buf + list reply。
- RESP: c->resp 2/3，默认 RESP2 (config.c:3341)；multibulk *count\r\n $len\r\n data (networking.c:3219)；inline ≤64KB。PROTO_REQ_INLINE=1/MULTIBULK=2。
- 命令表: redisCommandTable (server.c:3483)，commands.def MAKE_CMD；lookupCommand dictFetchValue (server.c:3599)。flags CMD_WRITE/READONLY/DENYOOM。
- call() 职责 (server.c:3958+)：dirty 追踪、耗时、slowlog (4079)、monitors (4090)、命令统计 calls/微秒/直方图 (4096)、传播 dirty→AOF|REPL (4131-4145)。
- serverCron (server.c:1557)，hz 默认=10 (server.h:118)，动态 hz 客户端多时翻倍；run_with_period 门控；clientsCron/databasesCron(activeExpireCycle SLOW+rehash+resize)；BGSAVE/AOF 触发。beforeSleep 里跑 FAST expire (server.c:1992) + 处理 IO 线程返回的 client + handleClientsWithPendingWrites。

## 内存/过期/淘汰（db.c/expire.c/evict.c/lazyfree.c/bio.c）
- 过期字典 db->expires 独立 kvstore (db.c:2753)；setExpire (db.c:2715)；getExpire (db.c:2771)。
- 惰性：expireIfNeeded 在 lookupKeyWithFlags (db.c:302,2944)；keyIsExpired now>when (db.c:2886)。
- 定期：activeExpireCycle (expire.c)。KEYS_PER_LOOP=20, FAST_DURATION=1000us, SLOW_TIME_PERC=25, ACCEPTABLE_STALE=10 (expire.c:96-99)；effort 自适应 (active-expire-effort 默认1, config.c:3318)；SLOW 预算=25%/hz-tick (expire.c:347)；25% continue-loop：expired*100/sampled>10 则重复 (expire.c:447)。
- 复制下过期：replica 不自行删，expireIfNeeded 返回 KEY_EXPIRED 逻辑过期但保留 key，等 master DEL/UNLINK (db.c:2983-2986)；master propagateDeletion 发 UNLINK/DEL (db.c:2863)。
- maxmemory 策略 (server.h:692-708)：8 种；默认 noeviction (config.c:3273)，maxmemory 默认0 (config.c:3367)。
- 近似 LRU：EVPOOL_SIZE=16 (evict.c:36)；采样 maxmemory-samples 默认5 (config.c:3304)；LRU_BITS=24，LRU_CLOCK_RESOLUTION=1000ms。
- LFU：24bit lru 拆 高16bit LDT(分钟) + 低8bit 计数；LFULogIncr 概率对数增 p=1/((c-5)*factor+1) 饱和255 (evict.c:281)；LFU_INIT_VAL=5；LFUDecrAndReturn 按 decay 衰减；lfu-log-factor 默认10, lfu-decay-time 默认1 (config.c:3300-3301)。
- 触发：processCommand 前 performEvictions (server.c:4608)；getMaxmemoryState mem_used=zmalloc_used - notCounted，mem_tofree (evict.c:384-414)；不计 replica 输出缓冲+AOF buf (evict.c:318)。
- lazyfree：4 配置默认0 (config.c:3195-3199)；freeObjAsync 仅 effort>64 && refcount==1 才交 bio (lazyfree.c:223)；BIO_LAZY_FREE。
- jemalloc 默认(Linux, Makefile:81)；zmalloc_used_memory；activedefrag 默认0 (config.c:3216)。

## 持久化（rdb.c/aof.c/bio.c/server.c）
- RDB: rdbSave (rdb.c:2027) 写 temp 再 rename+fsyncdir；SAVE 阻塞 (rdb.c:4825)；BGSAVE rdbSaveBackground fork (rdb.c:2070-2100)。
- RDB 格式：magic "REDIS%04d" RDB_VERSION=14 (rdb.c:1889, rdb.h:21)；opcodes SELECTDB254/EXPIRETIME_MS252/AUX250/RESIZEDB251/EOF255/FREQ249/IDLE248 (rdb.h:97-109)；CRC64 8字节尾 (rdb.c:1915)。
- save points: 3600/1, 300/100, 60/10000 (server.c:2462-2464)；serverCron dirty 检查 (server.c:1699)。
- AOF: feedAppendOnlyFile 执行后追加 aof_buf (aof.c:1409-1444)；flushAppendOnlyFile (aof.c:1147)；appendfsync 默认 everysec (config.c:3274)。
- everysec: ≥1000ms 且无进行中 fsync 则 aof_background_fsync→bio (aof.c:983,1347)；进行中最多推迟 2000ms 否则照写+aof_delayed_fsync (aof.c:1186-1204)→丢数据窗口 1-2s。
- AOF 重写: rewriteAppendOnlyFileBackground (aof.c:2867) flush→openNewIncrAof→fork；**无 aof_rewrite_buf**，父进程写新 INCR 文件 (aof.c:2856-2861)。
- Multi-Part AOF: .base/.incr/.manifest (aof.c:74-78)；appenddirname=appendonlydir (config.c:3250)；appendfilename=appendonly.aof。
- 混合: aof-use-rdb-preamble 默认yes (config.c:3208)，base 用 RDB 格式 (aof.c:2810)。
- fork/COW: redisFork (server.c:7437)；fork 耗时随数据集 (server.c:7479)；dict resize 避免以减 COW (server.c:1325)。
- bio: BIO_CLOSE_FILE/AOF_FSYNC/LAZY_FREE (bio.h:24-33)；bioSubmitJob。
- 加载优先级：appendonly on→AOF 优先，否则 RDB (server.c:7643-7663)；appendonly 默认0。
- auto-aof-rewrite-percentage=100 (config.c:3291), -min-size=64mb (config.c:3387)。

## 复制（replication.c）
- 握手：syncWithMaster 状态机 CONNECTING→...→PSYNC→CONNECTED (replication.c:3081-3203)；replica 发 PSYNC replid|? offset|-1 (replication.c:2910)；REPLCONF capa eof/psync2/ACK/GETACK (replication.c:1407-1619)。
- 全量：syncCommand (replication.c:1188)，master 回 +FULLRESYNC replid offset；diskless=repl_diskless_sync&&EOF cap→rdbSaveToSlavesSockets，否则磁盘 BGSAVE (replication.c:1103-1127)；replica 态 WAIT_BGSAVE_START→END→ONLINE。
- 部分重同步 PSYNC2：masterTryPartialResync (replication.c:986)，requested replid==replid 或 ==replid2 且 offset<=second_replid_offset 则 +CONTINUE，否则 need_full (replication.c:998-1021)；backlog 懒建 (replication.c:1292)；replid 40hex (server.h:132)；backlog init offset=master_repl_offset+1。
- 命令传播：replicationFeedSlaves (replication.c:631)；sub-replica 转发不重生成 (replication.c:644)；master_repl_offset 即使无从库也+1(为AOF)。异步，写不等 ack。
- WAIT (replication.c:4756)：等 N 个 replica ack offset；min-replicas-to-write 默认0/max-lag 默认10 (config.c:3320-3321)，server.c:4668 拒写。
- 默认：repl-diskless-sync=yes (config.c:3201), -delay=5, repl-backlog-size=1mb (config.c:3363), repl-ping-replica-period=10。

## 集群（cluster.c/cluster_legacy.c/sentinel.c）
- 16384 槽 CLUSTER_SLOTS (cluster.h:23)；keyHashSlot crc16&0x3FFF，hash tag {…} (cluster.c:33-61)。
- Gossip: PING0/PONG1/MEET2/FAIL3/FAILOVER_AUTH_REQUEST5/ACK6 (cluster_legacy.h:96)；总线端口=base+10000 (CLUSTER_PORT_INCR)。
- 迁移：MIGRATING/IMPORTING；getNodeByQuery→MOVED(n!=myself) / ASK(migrating_slot&&missing_keys) / TRYAGAIN (cluster.c:1402-1490)；importing 侧需 CLIENT_ASKING；SETSLOT NODE 完成。
- 故障转移：PFAIL4/FAIL8 (cluster_legacy.h:62)；markNodeAsFailingIfNeeded quorum=size/2+1 (cluster_legacy.c:1913-1930)；投票 failover_auth_count>=quorum (4508)；currentEpoch/configEpoch/lastVoteEpoch。
- 默认：cluster-enabled=0 immutable (config.c:3218), cluster-node-timeout=15000ms (config.c:3352)。
- Sentinel: SRI_S_DOWN(1<<3)主观/SRI_O_DOWN(1<<4)客观 (sentinel.c:47)；sentinelCheckObjectivelyDown quorum>=master->quorum (sentinel.c:4654-4675)；leader 选举 leader_epoch/sentinelVoteLeader。

## 事务/脚本/pubsub（multi.c/eval.c/script.c/functions.c/pubsub.c/notify.c/blocked.c）
- MULTI: CLIENT_MULTI (server.h:380)；queueMultiCommand (multi.c:35-75)；processCommand 排队 (server.c:4785)。DISCARD 清 mstate。
- WATCH: watchedKey (multi.c:276)；双链 c->watched_keys + db->watched_keys；touchWatchedKey 标 CLIENT_DIRTY_CAS (multi.c:387)；signalModifiedKey→touchWatchedKey (db.c:1180)；EXEC 若 DIRTY_CAS 返回 nil (multi.c:150)。
- 原子无回滚：DIRTY_EXEC→-EXECABORT(排队错)，DIRTY_CAS→nil；EXEC 设 CLIENT_DENY_BLOCKING (multi.c:164)；顺序 call 全部，运行时错不回滚 (multi.c:222)。
- EVAL: 缓存 lua_scripts SHA1→body (eval.c:47)；f_<sha> 注册；EVALSHA 无则 NOSCRIPT (eval.c:552-599)。
- 脚本原子 + 效果复制：scriptCall 传播执行的子命令非脚本本身 (script.c:681, eval.c:630)；多效果包 MULTI/EXEC (server.c:3827)；慢脚本 scriptInterrupt processEventsWhileBlocked (script.c:141)。
- FUNCTION (7.0+): functions.c；functionLoad (1044)；FCALL fcallCommand (619)；redis.register_function (function_lua.c:403)。
- KEYS/ARGV (script_lua.c:1682)；redis.call raise / pcall no-raise (script_lua.c:1018)；no-writes SCRIPT_FLAG (script.c:24)。
- Pub/Sub: pubsub_channels kvstore + pubsub_patterns dict (pubsub.c:61)；PUBLISH fire-and-forget 无持久化 (pubsub.c:469-535)；分片 SSUBSCRIBE/SPUBLISH 按 slot 忽略 pattern (pubsub.c:721-744)。
- 键空间通知: notifyKeyspaceEvent (notify.c:276)；__keyspace@db__:key / __keyevent@db__:event (notify.c:169-188)；notify-keyspace-events 标志 KEA g$lshzxet。
- 阻塞: blockClient CLIENT_BLOCKED (blocked.c:74)；blockForKeys db->blocking_keys (blocked.c:399)；signalKeyAsReady→server.ready_keys→handleClientsBlockedOnKeys (blocked.c:346)，processCommand 后触发 (server.c:4798)。BLPOP t_list.c:1371。
